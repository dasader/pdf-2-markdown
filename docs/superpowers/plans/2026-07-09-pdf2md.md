# pdf2md Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** PDF를 업로드하면 Docling으로 Markdown(+이미지 ZIP, 표 CSV)으로 변환하고, 다중 파일을 큐로 순차 처리하며 진행상황을 실시간 표시하는 저사양·멀티유저 웹서비스.

**Architecture:** 단일 Docker 이미지, compose 서비스 2개(`web`=FastAPI+정적UI, `worker`=Docling 1개). SQLite 파일 하나를 큐 겸 DB로 공유(WAL). 워커는 1초 폴링으로 `queued` 잡을 하나씩 집어 처리. 진행상황은 SSE. 로그인 없이 쿠키 세션으로 사용자 구분, `X-Admin-Key`로 전체 조회.

**Tech Stack:** Python 3.12, FastAPI, uvicorn, Docling(CPU, OCR off, TableFormer ACCURATE), PyMuPDF(페이지 수·매직바이트), SQLite(stdlib), 정적 HTML/CSS/JS(빌드 없음), vendored marked.js.

## Global Constraints

- Python **3.12-slim** 베이스. Redis·Celery·Postgres·nginx·Node 도입 금지.
- 워커는 **1개**. 순차 처리가 요구사항이자 메모리 상한 장치.
- Docling: `do_ocr=False`, `do_table_structure=True`, `table_structure_options.mode=TableFormerMode.ACCURATE`.
- docling import는 **함수 내부 지연 import** — 테스트가 torch 없이 돌아야 함.
- 가드레일(축소 금지): 파일당 **100MB / 500페이지** 상한, 매직바이트 `%PDF` 검사, 저장 파일명은 항상 sha256(요청 경로 신뢰 금지), 세션당 대기 잡 **20개** 상한.
- 포트: web **8001** (NN=01). `.env`로 `${PDF2MD_PORT:-8001}` 오버라이드.
- 보관 24시간. 파일 삭제는 **참조 카운트**(잡 행 삭제 → 고아 파일 스윕).
- 저장 배치: `data/uploads/<sha>.pdf`, `data/results/<sha>-<opts>/{doc.md, images/, tables/*.csv, result.zip}`.
- 커밋 자주. 각 태스크 끝에서 커밋.

## File Structure

```
pdf2md/
  requirements.txt          # 런타임 의존성
  Dockerfile                # 단일 이미지
  docker-compose.yml        # web + worker 2 서비스
  .env.example
  app/
    __init__.py
    config.py               # 환경변수·경로·상한 (web/worker 공유)
    db.py                   # SQLite 스키마·연결·잡 CRUD·캐시조회·정리
    convert.py              # Docling 변환 + ZIP 패키징 (핵심, 순수)
    worker.py               # 폴링 루프: 잡 선점→변환→상태갱신, 정리 스윕
    web.py                  # FastAPI: 업로드/목록/다운로드/미리보기/SSE + 정적
  static/
    index.html
    style.css
    app.js
    marked.min.js           # vendored (Task 5)
  tests/
    __init__.py
    test_pdf2md.py          # 캐시히트·비PDF거부·상태전이·세션격리
    fixtures/
      sample.pdf            # 1페이지 텍스트 PDF (Task 2에서 생성)
```

---

### Task 1: 설정 + DB 계층

**Files:**
- Create: `app/__init__.py` (빈 파일)
- Create: `app/config.py`
- Create: `app/db.py`
- Create: `tests/__init__.py` (빈 파일)
- Test: `tests/test_pdf2md.py`

**Interfaces:**
- Produces (`app/config.py`): 모듈 상수 `DATA_DIR`, `UPLOADS_DIR`, `RESULTS_DIR`, `DB_PATH`, `ADMIN_KEY: str`, `MAX_BYTES=100*1024*1024`, `MAX_PAGES=500`, `MAX_QUEUED_PER_SESSION=20`, `SEC_PER_PAGE: float`, `RETENTION_SEC=24*3600`, `PORT: int`. 함수 `ensure_dirs() -> None`.
- Produces (`app/db.py`):
  - `connect() -> sqlite3.Connection` (WAL, `row_factory=sqlite3.Row`)
  - `init_db() -> None`
  - `create_job(conn, *, id, session_id, filename, sha256, opts_hash, status, page_total, result_dir) -> None`
  - `find_cached(conn, sha256, opts_hash) -> sqlite3.Row | None` (status='done'인 최신 잡)
  - `get_job(conn, job_id) -> sqlite3.Row | None`
  - `list_jobs(conn, session_id, admin: bool) -> list[sqlite3.Row]`
  - `claim_next_queued(conn) -> sqlite3.Row | None` (원자적 queued→running)
  - `finish_job(conn, job_id, *, status, error=None, result_dir=None) -> None`
  - `count_queued(conn, session_id) -> int`
  - `expired_job_ids(conn, now: float) -> list[str]` / `delete_jobs(conn, ids) -> None`
  - `referenced_shas(conn) -> set[str]` / `referenced_result_dirs(conn) -> set[str]`
  - `now() -> float` (time.time 래퍼 — 테스트에서 monkeypatch)

- [ ] **Step 1: 실패 테스트 작성** — `tests/test_pdf2md.py`

```python
import time
import pytest
from app import config, db


@pytest.fixture
def conn(tmp_path, monkeypatch):
    monkeypatch.setattr(config, "DATA_DIR", tmp_path)
    monkeypatch.setattr(config, "DB_PATH", tmp_path / "app.db")
    monkeypatch.setattr(config, "UPLOADS_DIR", tmp_path / "uploads")
    monkeypatch.setattr(config, "RESULTS_DIR", tmp_path / "results")
    config.ensure_dirs()
    db.init_db()
    c = db.connect()
    yield c
    c.close()


def _job(conn, jid, session="s1", sha="abc", opts="o1", status="queued", result_dir=None):
    db.create_job(conn, id=jid, session_id=session, filename="f.pdf", sha256=sha,
                  opts_hash=opts, status=status, page_total=3, result_dir=result_dir)


def test_create_and_get_job(conn):
    _job(conn, "j1")
    row = db.get_job(conn, "j1")
    assert row["status"] == "queued"
    assert row["filename"] == "f.pdf"


def test_list_jobs_session_isolation(conn):
    _job(conn, "j1", session="s1")
    _job(conn, "j2", session="s2")
    mine = db.list_jobs(conn, "s1", admin=False)
    assert [r["id"] for r in mine] == ["j1"]
    all_jobs = db.list_jobs(conn, "s1", admin=True)
    assert {r["id"] for r in all_jobs} == {"j1", "j2"}


def test_find_cached_returns_done_only(conn):
    _job(conn, "j1", sha="X", opts="O", status="queued")
    assert db.find_cached(conn, "X", "O") is None
    _job(conn, "j2", sha="X", opts="O", status="done", result_dir="/r/X-O")
    hit = db.find_cached(conn, "X", "O")
    assert hit["result_dir"] == "/r/X-O"


def test_claim_next_queued_atomic(conn):
    _job(conn, "j1", status="queued")
    claimed = db.claim_next_queued(conn)
    assert claimed["id"] == "j1"
    assert db.get_job(conn, "j1")["status"] == "running"
    assert db.claim_next_queued(conn) is None  # 더 없음


def test_count_queued(conn):
    _job(conn, "j1", session="s1", status="queued")
    _job(conn, "j2", session="s1", status="done")
    assert db.count_queued(conn, "s1") == 1
```

- [ ] **Step 2: 실패 확인**

Run: `cd pdf2md && python -m pytest tests/test_pdf2md.py -x -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.config'` (또는 config)

- [ ] **Step 3: `app/config.py` 작성**

```python
import os
from pathlib import Path

DATA_DIR = Path(os.environ.get("PDF2MD_DATA", "data"))
DB_PATH = DATA_DIR / "app.db"
UPLOADS_DIR = DATA_DIR / "uploads"
RESULTS_DIR = DATA_DIR / "results"

ADMIN_KEY = os.environ.get("PDF2MD_ADMIN_KEY", "")
PORT = int(os.environ.get("PDF2MD_PORT", "8001"))

MAX_BYTES = 100 * 1024 * 1024
MAX_PAGES = 500
MAX_QUEUED_PER_SESSION = 20
SEC_PER_PAGE = float(os.environ.get("PDF2MD_SEC_PER_PAGE", "1.5"))
RETENTION_SEC = 24 * 3600


def ensure_dirs() -> None:
    for d in (DATA_DIR, UPLOADS_DIR, RESULTS_DIR):
        d.mkdir(parents=True, exist_ok=True)
```

- [ ] **Step 4: `app/db.py` 작성**

```python
import sqlite3
import time
from app import config

SCHEMA = """
CREATE TABLE IF NOT EXISTS jobs (
  id          TEXT PRIMARY KEY,
  session_id  TEXT NOT NULL,
  filename    TEXT NOT NULL,
  sha256      TEXT NOT NULL,
  opts_hash   TEXT NOT NULL,
  status      TEXT NOT NULL,
  page_total  INTEGER,
  started_at  REAL,
  finished_at REAL,
  error       TEXT,
  result_dir  TEXT,
  created_at  REAL NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_jobs_status  ON jobs(status);
CREATE INDEX IF NOT EXISTS idx_jobs_cache   ON jobs(sha256, opts_hash, status);
CREATE INDEX IF NOT EXISTS idx_jobs_session ON jobs(session_id, created_at);
"""


def now() -> float:
    return time.time()


def connect() -> sqlite3.Connection:
    conn = sqlite3.connect(config.DB_PATH, timeout=30)
    conn.row_factory = sqlite3.Row
    conn.execute("PRAGMA journal_mode=WAL")
    conn.execute("PRAGMA busy_timeout=30000")
    return conn


def init_db() -> None:
    conn = connect()
    try:
        conn.executescript(SCHEMA)
        conn.commit()
    finally:
        conn.close()


def create_job(conn, *, id, session_id, filename, sha256, opts_hash,
               status, page_total, result_dir=None) -> None:
    conn.execute(
        "INSERT INTO jobs (id, session_id, filename, sha256, opts_hash, status, "
        "page_total, result_dir, created_at) VALUES (?,?,?,?,?,?,?,?,?)",
        (id, session_id, filename, sha256, opts_hash, status, page_total, result_dir, now()),
    )
    conn.commit()


def find_cached(conn, sha256, opts_hash):
    return conn.execute(
        "SELECT * FROM jobs WHERE sha256=? AND opts_hash=? AND status='done' "
        "ORDER BY created_at DESC LIMIT 1",
        (sha256, opts_hash),
    ).fetchone()


def get_job(conn, job_id):
    return conn.execute("SELECT * FROM jobs WHERE id=?", (job_id,)).fetchone()


def list_jobs(conn, session_id, admin: bool):
    if admin:
        return conn.execute(
            "SELECT * FROM jobs ORDER BY created_at DESC LIMIT 200").fetchall()
    return conn.execute(
        "SELECT * FROM jobs WHERE session_id=? ORDER BY created_at DESC LIMIT 200",
        (session_id,)).fetchall()


def claim_next_queued(conn):
    # 원자적 선점: 다중 워커로 늘려도 안전.
    cur = conn.execute(
        "UPDATE jobs SET status='running', started_at=? "
        "WHERE id = (SELECT id FROM jobs WHERE status='queued' "
        "            ORDER BY created_at LIMIT 1) RETURNING *",
        (now(),),
    )
    row = cur.fetchone()
    conn.commit()
    return row


def finish_job(conn, job_id, *, status, error=None, result_dir=None) -> None:
    conn.execute(
        "UPDATE jobs SET status=?, error=?, result_dir=COALESCE(?, result_dir), "
        "finished_at=? WHERE id=?",
        (status, error, result_dir, now(), job_id),
    )
    conn.commit()


def count_queued(conn, session_id) -> int:
    return conn.execute(
        "SELECT COUNT(*) FROM jobs WHERE session_id=? AND status='queued'",
        (session_id,)).fetchone()[0]


def expired_job_ids(conn, now_ts):
    rows = conn.execute(
        "SELECT id FROM jobs WHERE created_at < ?",
        (now_ts - config.RETENTION_SEC,)).fetchall()
    return [r["id"] for r in rows]


def delete_jobs(conn, ids) -> None:
    conn.executemany("DELETE FROM jobs WHERE id=?", [(i,) for i in ids])
    conn.commit()


def referenced_shas(conn):
    return {r["sha256"] for r in conn.execute("SELECT DISTINCT sha256 FROM jobs")}


def referenced_result_dirs(conn):
    return {r["result_dir"] for r in
            conn.execute("SELECT DISTINCT result_dir FROM jobs WHERE result_dir IS NOT NULL")}
```

> 주: `RETURNING *`는 SQLite 3.35+(Python 3.12 동봉분에 포함). 안전.

- [ ] **Step 5: 통과 확인**

Run: `cd pdf2md && python -m pytest tests/test_pdf2md.py -x -q`
Expected: PASS (5 passed)

- [ ] **Step 6: 커밋**

```bash
git add app/__init__.py app/config.py app/db.py tests/__init__.py tests/test_pdf2md.py
git commit -m "feat: 설정·SQLite 잡 큐 DB 계층"
```

---

### Task 2: Docling 변환 + ZIP 패키징 (핵심)

**Files:**
- Create: `app/convert.py`
- Create: `tests/fixtures/sample.pdf` (아래 스크립트로 생성)
- Modify: `tests/test_pdf2md.py` (변환 테스트 추가)

**Interfaces:**
- Consumes: `app.config` 경로 상수.
- Produces (`app/convert.py`):
  - `sha256_file(path) -> str`
  - `is_pdf(head: bytes) -> bool` (매직바이트 `%PDF`)
  - `page_count(path) -> int` (PyMuPDF)
  - `opts_hash(include_images: bool, include_tables_csv: bool) -> str`
  - `convert(pdf_path, out_dir, *, include_images: bool, include_tables_csv: bool) -> None`
    — `out_dir`에 `doc.md`, (옵션)`images/`, (옵션)`tables/*.csv`, `result.zip` 생성. docling은 함수 내부에서 import.

- [ ] **Step 1: 픽스처 PDF 생성 스크립트 실행**

Run:
```bash
cd pdf2md && mkdir -p tests/fixtures && python -c "
import fitz
d = fitz.open()
p = d.new_page()
p.insert_text((72, 72), 'Hello pdf2md\nsecond line')
d.save('tests/fixtures/sample.pdf'); d.close()
print('ok')
"
```
Expected: `ok`, `tests/fixtures/sample.pdf` 생성. (PyMuPDF=`fitz` 필요 — `pip install pymupdf`)

- [ ] **Step 2: 실패 테스트 작성** — `tests/test_pdf2md.py` 하단에 추가

```python
from pathlib import Path
from app import convert

FIX = Path(__file__).parent / "fixtures" / "sample.pdf"


def test_is_pdf_magic_bytes():
    assert convert.is_pdf(b"%PDF-1.7 ...")
    assert not convert.is_pdf(b"PK\x03\x04zip")


def test_page_count(_=None):
    assert convert.page_count(FIX) == 1


def test_opts_hash_stable_and_distinct():
    a = convert.opts_hash(True, True)
    assert a == convert.opts_hash(True, True)
    assert a != convert.opts_hash(False, True)
    assert a != convert.opts_hash(True, False)


def test_convert_packages_zip(tmp_path, monkeypatch):
    # docling을 가짜로 대체: doc.md만 쓰고 tables 없음.
    class FakeDoc:
        tables = []
        def save_as_markdown(self, path, image_mode=None):
            Path(path).write_text("# hi\n")
    class FakeResult:
        document = FakeDoc()
    class FakeConverter:
        def __init__(self, *a, **k): pass
        def convert(self, p): return FakeResult()

    monkeypatch.setattr(convert, "_build_converter", lambda: FakeConverter())
    out = tmp_path / "X-O"
    convert.convert(FIX, out, include_images=True, include_tables_csv=True)
    assert (out / "doc.md").exists()
    assert (out / "result.zip").exists()
    import zipfile
    names = zipfile.ZipFile(out / "result.zip").namelist()
    assert "doc.md" in names
```

- [ ] **Step 3: 실패 확인**

Run: `cd pdf2md && python -m pytest tests/test_pdf2md.py -x -q -k "convert or pdf or page or opts"`
Expected: FAIL — `AttributeError: module 'app.convert' has no attribute ...`

- [ ] **Step 4: `app/convert.py` 작성**

```python
import hashlib
import shutil
import zipfile
from pathlib import Path

import fitz  # PyMuPDF


def sha256_file(path) -> str:
    h = hashlib.sha256()
    with open(path, "rb") as f:
        for chunk in iter(lambda: f.read(1 << 20), b""):
            h.update(chunk)
    return h.hexdigest()


def is_pdf(head: bytes) -> bool:
    return head[:5] == b"%PDF-"


def page_count(path) -> int:
    with fitz.open(path) as doc:
        return doc.page_count


def opts_hash(include_images: bool, include_tables_csv: bool) -> str:
    key = f"img={int(include_images)};csv={int(include_tables_csv)}"
    return hashlib.sha256(key.encode()).hexdigest()[:16]


def _build_converter():
    # 지연 import: 테스트가 torch 없이 돌게 함.
    from docling.datamodel.base_models import InputFormat
    from docling.datamodel.pipeline_options import PdfPipelineOptions, TableFormerMode
    from docling.document_converter import DocumentConverter, PdfFormatOption

    opts = PdfPipelineOptions()
    opts.do_ocr = False                       # 텍스트 PDF → OCR 모델 미로딩(~2GB 절감)
    opts.do_table_structure = True
    opts.table_structure_options.mode = TableFormerMode.ACCURATE
    opts.images_scale = 2.0
    opts.generate_picture_images = True
    return DocumentConverter(
        format_options={InputFormat.PDF: PdfFormatOption(pipeline_options=opts)}
    )


def convert(pdf_path, out_dir, *, include_images: bool, include_tables_csv: bool) -> None:
    from docling_core.types.doc import ImageRefMode

    out_dir = Path(out_dir)
    out_dir.mkdir(parents=True, exist_ok=True)
    md_path = out_dir / "doc.md"

    result = _build_converter().convert(str(pdf_path))
    doc = result.document

    image_mode = ImageRefMode.REFERENCED if include_images else ImageRefMode.PLACEHOLDER
    doc.save_as_markdown(str(md_path), image_mode=image_mode)

    # save_as_markdown(REFERENCED)는 doc_artifacts/ 폴더에 이미지를 쓴다 → images/로 정규화
    artifacts = out_dir / f"{md_path.stem}_artifacts"
    images_dir = out_dir / "images"
    if include_images and artifacts.exists():
        if images_dir.exists():
            shutil.rmtree(images_dir)
        artifacts.rename(images_dir)
        # md 안의 참조 경로 치환
        md = md_path.read_text(encoding="utf-8").replace(
            f"{md_path.stem}_artifacts/", "images/")
        md_path.write_text(md, encoding="utf-8")

    tables_dir = out_dir / "tables"
    if include_tables_csv and getattr(doc, "tables", None):
        tables_dir.mkdir(exist_ok=True)
        for i, table in enumerate(doc.tables, 1):
            df = table.export_to_dataframe(doc=doc)
            df.to_csv(tables_dir / f"table-{i:02d}.csv", index=False)

    # ZIP 패키징
    zip_path = out_dir / "result.zip"
    with zipfile.ZipFile(zip_path, "w", zipfile.ZIP_DEFLATED) as z:
        z.write(md_path, "doc.md")
        for sub in ("images", "tables"):
            d = out_dir / sub
            if d.exists():
                for f in sorted(d.rglob("*")):
                    if f.is_file():
                        z.write(f, str(f.relative_to(out_dir)))
```

> 주: `export_to_dataframe(doc=doc)` 시그니처는 docling 버전에 따라 인자 없이도 동작. 실패 시 `table.export_to_dataframe()`로 폴백해도 무방(런타임에서 확인).

- [ ] **Step 5: 통과 확인**

Run: `cd pdf2md && python -m pytest tests/test_pdf2md.py -x -q -k "convert or pdf or page or opts"`
Expected: PASS

- [ ] **Step 6: 커밋**

```bash
git add app/convert.py tests/test_pdf2md.py tests/fixtures/sample.pdf
git commit -m "feat: Docling 변환·이미지/CSV 추출·ZIP 패키징"
```

---

### Task 3: 워커 폴링 루프 + 정리

**Files:**
- Create: `app/worker.py`
- Modify: `tests/test_pdf2md.py`

**Interfaces:**
- Consumes: `db.claim_next_queued/finish_job/expired_job_ids/delete_jobs/referenced_shas/referenced_result_dirs`, `convert.convert`.
- Produces (`app/worker.py`):
  - `process_one(conn) -> bool` — 잡 하나 처리(있으면 True). 캐시된 잡은 이미 done이므로 큐에 없음. 변환 실패 시 `failed`+error.
  - `sweep(conn) -> None` — 만료 잡 삭제 후 고아 upload/result 파일 정리.
  - `run() -> None` — `while True: process_one; 주기적 sweep; sleep(1)` (엔트리포인트).

- [ ] **Step 1: 실패 테스트 작성** — 하단 추가

```python
from app import worker


def test_process_one_success(conn, tmp_path, monkeypatch):
    _job(conn, "j1", sha="Y", opts="O", status="queued")
    # 업로드 파일 존재해야 함
    (config.UPLOADS_DIR / "Y.pdf").write_bytes(b"%PDF-1.7")
    called = {}
    def fake_convert(pdf, out, **kw):
        called["out"] = str(out); Path(out).mkdir(parents=True, exist_ok=True)
    monkeypatch.setattr(worker.convert, "convert", fake_convert)
    monkeypatch.setattr(worker.convert, "opts_hash", lambda *a: "O")

    assert worker.process_one(conn) is True
    row = db.get_job(conn, "j1")
    assert row["status"] == "done"
    assert row["result_dir"] == called["out"]


def test_process_one_failure_records_error(conn, monkeypatch):
    _job(conn, "j1", sha="Z", opts="O", status="queued")
    (config.UPLOADS_DIR / "Z.pdf").write_bytes(b"%PDF-1.7")
    def boom(*a, **k): raise RuntimeError("docling exploded")
    monkeypatch.setattr(worker.convert, "convert", boom)
    monkeypatch.setattr(worker.convert, "opts_hash", lambda *a: "O")

    assert worker.process_one(conn) is True
    row = db.get_job(conn, "j1")
    assert row["status"] == "failed"
    assert "docling exploded" in row["error"]


def test_process_one_empty(conn):
    assert worker.process_one(conn) is False


def test_sweep_deletes_expired_and_orphans(conn, monkeypatch):
    # 오래된 잡 + 그 파일
    (config.UPLOADS_DIR / "OLD.pdf").write_bytes(b"%PDF")
    old_res = config.RESULTS_DIR / "OLD-O"; old_res.mkdir()
    (old_res / "doc.md").write_text("x")
    _job(conn, "old", sha="OLD", opts="O", status="done", result_dir=str(old_res))
    # created_at을 과거로
    conn.execute("UPDATE jobs SET created_at=? WHERE id='old'",
                 (db.now() - config.RETENTION_SEC - 10,)); conn.commit()

    worker.sweep(conn)
    assert db.get_job(conn, "old") is None
    assert not (config.UPLOADS_DIR / "OLD.pdf").exists()
    assert not old_res.exists()
```

- [ ] **Step 2: 실패 확인**

Run: `cd pdf2md && python -m pytest tests/test_pdf2md.py -x -q -k "process_one or sweep"`
Expected: FAIL — `ModuleNotFoundError: app.worker`

- [ ] **Step 3: `app/worker.py` 작성**

```python
import shutil
import time
import traceback
from pathlib import Path

from app import config, db, convert

_SWEEP_EVERY = 300  # 초


def process_one(conn) -> bool:
    job = db.claim_next_queued(conn)
    if job is None:
        return False
    sha, opts = job["sha256"], job["opts_hash"]
    pdf_path = config.UPLOADS_DIR / f"{sha}.pdf"
    out_dir = config.RESULTS_DIR / f"{sha}-{opts}"
    include_images = job["opts_hash"] == convert.opts_hash(True, True) or \
        job["opts_hash"] == convert.opts_hash(True, False)
    include_csv = job["opts_hash"] == convert.opts_hash(True, True) or \
        job["opts_hash"] == convert.opts_hash(False, True)
    try:
        convert.convert(pdf_path, out_dir,
                        include_images=include_images, include_tables_csv=include_csv)
        db.finish_job(conn, job["id"], status="done", result_dir=str(out_dir))
    except Exception:
        db.finish_job(conn, job["id"], status="failed",
                      error=traceback.format_exc(limit=3))
    return True


def sweep(conn) -> None:
    ids = db.expired_job_ids(conn, db.now())
    if ids:
        db.delete_jobs(conn, ids)
    # 고아 파일 정리: 참조되지 않는 upload/result 삭제
    keep_shas = db.referenced_shas(conn)
    for f in config.UPLOADS_DIR.glob("*.pdf"):
        if f.stem not in keep_shas:
            f.unlink(missing_ok=True)
    keep_dirs = {Path(d).name for d in db.referenced_result_dirs(conn)}
    for d in config.RESULTS_DIR.iterdir():
        if d.is_dir() and d.name not in keep_dirs:
            shutil.rmtree(d, ignore_errors=True)


def run() -> None:
    config.ensure_dirs()
    db.init_db()
    conn = db.connect()
    last_sweep = 0.0
    while True:
        worked = process_one(conn)
        now = time.time()
        if now - last_sweep > _SWEEP_EVERY:
            sweep(conn); last_sweep = now
        if not worked:
            time.sleep(1)


if __name__ == "__main__":
    run()
```

> `include_images`/`include_csv`를 opts_hash 역산으로 구하는 게 지저분하다. Task 4에서 잡 생성 시 옵션을 결정하므로, 실제로는 opts_hash가 4가지 조합 중 하나다. 깔끔하게 하려면 opts를 잡 행에 별도 컬럼으로 둘 수 있으나 YAGNI — 4-조합 역산으로 충분. `# ponytail: opts 4조합 역산, 조합이 늘면 컬럼 추가`

- [ ] **Step 4: 통과 확인**

Run: `cd pdf2md && python -m pytest tests/test_pdf2md.py -x -q -k "process_one or sweep"`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add app/worker.py tests/test_pdf2md.py
git commit -m "feat: 워커 폴링 루프·변환 처리·보관 정리 스윕"
```

---

### Task 4: FastAPI 웹 — 업로드·목록·다운로드·미리보기·SSE

**Files:**
- Create: `app/web.py`
- Modify: `tests/test_pdf2md.py`
- Modify: `requirements.txt` (Task 6에서 최종화, 여기선 로컬 설치)

**Interfaces:**
- Consumes: `db.*`, `convert.sha256_file/is_pdf/page_count/opts_hash`, `config.*`.
- Produces (`app/web.py`): `app` (FastAPI 인스턴스). 라우트:
  - `GET /` → `static/index.html`
  - `POST /api/jobs` (multipart `files[]`, form `include_images`, `include_tables_csv`) → 생성 잡 목록 JSON. 세션 쿠키 없으면 발급.
  - `GET /api/jobs` → 잡 목록(본인/admin). 진행률 필드 계산 포함.
  - `GET /api/jobs/{id}/preview` → doc.md 원문 text/plain
  - `GET /api/jobs/{id}/download` → result.zip
  - `GET /api/events` → SSE (0.5초 폴링, 세션 잡 변경분)
  - 정적: `/static` 마운트
- 세션 쿠키: `sid`, httpOnly, SameSite=Lax. admin: `X-Admin-Key` 헤더 == `config.ADMIN_KEY`(비어있으면 항상 False).

- [ ] **Step 1: 실패 테스트 작성** — 하단 추가. `TestClient` 사용.

```python
from fastapi.testclient import TestClient


@pytest.fixture
def client(tmp_path, monkeypatch):
    monkeypatch.setattr(config, "DATA_DIR", tmp_path)
    monkeypatch.setattr(config, "DB_PATH", tmp_path / "app.db")
    monkeypatch.setattr(config, "UPLOADS_DIR", tmp_path / "uploads")
    monkeypatch.setattr(config, "RESULTS_DIR", tmp_path / "results")
    monkeypatch.setattr(config, "ADMIN_KEY", "secret")
    config.ensure_dirs(); db.init_db()
    from app import web
    return TestClient(web.app)


def _pdf_bytes():
    return (FIX).read_bytes()


def test_upload_creates_queued_job(client):
    r = client.post("/api/jobs",
                    files={"files": ("a.pdf", _pdf_bytes(), "application/pdf")},
                    data={"include_images": "true", "include_tables_csv": "true"})
    assert r.status_code == 200
    jobs = r.json()
    assert len(jobs) == 1 and jobs[0]["status"] == "queued"
    assert "sid" in r.cookies


def test_upload_rejects_non_pdf(client):
    r = client.post("/api/jobs",
                    files={"files": ("x.pdf", b"PK\x03\x04not a pdf", "application/pdf")},
                    data={"include_images": "true", "include_tables_csv": "true"})
    jobs = r.json()
    assert jobs[0]["status"] == "failed"
    assert "PDF" in (jobs[0]["error"] or "")


def test_cache_hit_second_upload_skips(client):
    f = {"files": ("a.pdf", _pdf_bytes(), "application/pdf")}
    d = {"include_images": "true", "include_tables_csv": "true"}
    r1 = client.post("/api/jobs", files=f, data=d)
    # 첫 잡을 done으로 만들고 결과 디렉토리 생성
    conn = db.connect()
    j1 = r1.json()[0]
    res_dir = config.RESULTS_DIR / f"{j1['sha256']}-{j1['opts_hash']}"
    res_dir.mkdir(parents=True); (res_dir / "doc.md").write_text("# x")
    db.finish_job(conn, j1["id"], status="done", result_dir=str(res_dir)); conn.close()
    r2 = client.post("/api/jobs", files={"files": ("a.pdf", _pdf_bytes(), "application/pdf")}, data=d)
    assert r2.json()[0]["status"] == "done"  # 캐시 히트 → 즉시 done


def test_session_isolation_download_404(client):
    r1 = client.post("/api/jobs", files={"files": ("a.pdf", _pdf_bytes(), "application/pdf")},
                     data={"include_images": "true", "include_tables_csv": "true"})
    jid = r1.json()[0]["id"]
    other = TestClient(client.app)  # 새 세션
    assert other.get(f"/api/jobs/{jid}/download").status_code == 404


def test_admin_key_sees_all(client):
    client.post("/api/jobs", files={"files": ("a.pdf", _pdf_bytes(), "application/pdf")},
                data={"include_images": "true", "include_tables_csv": "true"})
    other = TestClient(client.app)
    r = other.get("/api/jobs", headers={"X-Admin-Key": "secret"})
    assert len(r.json()) >= 1
```

- [ ] **Step 2: 실패 확인**

Run: `cd pdf2md && pip install fastapi 'uvicorn[standard]' python-multipart httpx && python -m pytest tests/test_pdf2md.py -x -q -k "upload or cache_hit or session_isolation or admin"`
Expected: FAIL — `app.web` 없음

- [ ] **Step 3: `app/web.py` 작성**

```python
import time
import uuid
import json
from pathlib import Path

from fastapi import FastAPI, UploadFile, Form, Request, Response
from fastapi.responses import (
    JSONResponse, PlainTextResponse, FileResponse, StreamingResponse, HTMLResponse,
)
from fastapi.staticfiles import StaticFiles

from app import config, db, convert

STATIC = Path(__file__).parent.parent / "static"

app = FastAPI()
app.mount("/static", StaticFiles(directory=STATIC), name="static")


def _sid(request: Request) -> str:
    return request.cookies.get("sid") or uuid.uuid4().hex


def _is_admin(request: Request) -> bool:
    key = request.headers.get("X-Admin-Key", "")
    return bool(config.ADMIN_KEY) and key == config.ADMIN_KEY


def _row_to_dict(r) -> dict:
    d = dict(r)
    # 진행률(추정): running이면 경과/예상, 그 외 상태 기반
    if d["status"] == "done":
        d["progress"] = 100
    elif d["status"] == "running" and d.get("started_at"):
        est = max(1.0, (d.get("page_total") or 1) * config.SEC_PER_PAGE)
        d["progress"] = min(95, int((time.time() - d["started_at"]) / est * 100))
    else:
        d["progress"] = 0
    d.pop("session_id", None)
    return d


@app.get("/", response_class=HTMLResponse)
def index():
    return (STATIC / "index.html").read_text(encoding="utf-8")


@app.post("/api/jobs")
async def create_jobs(request: Request, response: Response,
                      files: list[UploadFile] = None,
                      include_images: str = Form("true"),
                      include_tables_csv: str = Form("true")):
    sid = _sid(request)
    inc_img = include_images == "true"
    inc_csv = include_tables_csv == "true"
    oh = convert.opts_hash(inc_img, inc_csv)
    conn = db.connect()
    out = []
    try:
        for uf in (files or []):
            data = await uf.read()
            jid = uuid.uuid4().hex
            # 가드레일
            if len(data) > config.MAX_BYTES:
                db.create_job(conn, id=jid, session_id=sid, filename=uf.filename,
                              sha256="-", opts_hash=oh, status="failed", page_total=None)
                db.finish_job(conn, jid, status="failed", error="파일이 100MB를 초과합니다")
                out.append(db.get_job(conn, jid)); continue
            if not convert.is_pdf(data[:5]):
                db.create_job(conn, id=jid, session_id=sid, filename=uf.filename,
                              sha256="-", opts_hash=oh, status="failed", page_total=None)
                db.finish_job(conn, jid, status="failed", error="PDF 파일이 아닙니다")
                out.append(db.get_job(conn, jid)); continue
            if db.count_queued(conn, sid) >= config.MAX_QUEUED_PER_SESSION:
                db.create_job(conn, id=jid, session_id=sid, filename=uf.filename,
                              sha256="-", opts_hash=oh, status="failed", page_total=None)
                db.finish_job(conn, jid, status="failed", error="대기 잡이 너무 많습니다(최대 20)")
                out.append(db.get_job(conn, jid)); continue

            sha = convert._sha256_bytes(data)
            pdf_path = config.UPLOADS_DIR / f"{sha}.pdf"
            if not pdf_path.exists():
                pdf_path.write_bytes(data)
            try:
                pages = convert.page_count(pdf_path)
            except Exception:
                db.create_job(conn, id=jid, session_id=sid, filename=uf.filename,
                              sha256=sha, opts_hash=oh, status="failed", page_total=None)
                db.finish_job(conn, jid, status="failed", error="PDF를 열 수 없습니다")
                out.append(db.get_job(conn, jid)); continue
            if pages > config.MAX_PAGES:
                db.create_job(conn, id=jid, session_id=sid, filename=uf.filename,
                              sha256=sha, opts_hash=oh, status="failed", page_total=pages)
                db.finish_job(conn, jid, status="failed", error="500페이지를 초과합니다")
                out.append(db.get_job(conn, jid)); continue

            cached = db.find_cached(conn, sha, oh)
            if cached:
                db.create_job(conn, id=jid, session_id=sid, filename=uf.filename,
                              sha256=sha, opts_hash=oh, status="done", page_total=pages,
                              result_dir=cached["result_dir"])
            else:
                db.create_job(conn, id=jid, session_id=sid, filename=uf.filename,
                              sha256=sha, opts_hash=oh, status="queued", page_total=pages)
            out.append(db.get_job(conn, jid))
    finally:
        conn.close()
    response = JSONResponse([_row_to_dict(r) for r in out])
    response.set_cookie("sid", sid, httponly=True, samesite="lax", max_age=30 * 86400)
    return response


@app.get("/api/jobs")
def list_jobs(request: Request):
    conn = db.connect()
    try:
        rows = db.list_jobs(conn, _sid(request), admin=_is_admin(request))
        return JSONResponse([_row_to_dict(r) for r in rows])
    finally:
        conn.close()


def _owned(request, job_id):
    conn = db.connect()
    try:
        row = db.get_job(conn, job_id)
        if row is None:
            return None
        if row["session_id"] != _sid(request) and not _is_admin(request):
            return None
        return row
    finally:
        conn.close()


@app.get("/api/jobs/{job_id}/preview")
def preview(request: Request, job_id: str):
    row = _owned(request, job_id)
    if not row or row["status"] != "done":
        return PlainTextResponse("not found", status_code=404)
    md = Path(row["result_dir"]) / "doc.md"
    if not md.exists():
        return PlainTextResponse("not found", status_code=404)
    return PlainTextResponse(md.read_text(encoding="utf-8"))


@app.get("/api/jobs/{job_id}/download")
def download(request: Request, job_id: str):
    row = _owned(request, job_id)
    if not row or row["status"] != "done":
        return PlainTextResponse("not found", status_code=404)
    zp = Path(row["result_dir"]) / "result.zip"
    if not zp.exists():
        return PlainTextResponse("not found", status_code=404)
    name = Path(row["filename"]).stem + ".zip"
    return FileResponse(zp, filename=name, media_type="application/zip")


@app.get("/api/events")
def events(request: Request):
    sid = _sid(request)
    admin = _is_admin(request)

    def gen():
        seen = {}
        for _ in range(600):  # 최대 5분 후 재연결 유도
            conn = db.connect()
            try:
                rows = db.list_jobs(conn, sid, admin=admin)
            finally:
                conn.close()
            changed = []
            for r in rows:
                key = (r["status"], r["finished_at"])
                if seen.get(r["id"]) != key:
                    seen[r["id"]] = key
                    changed.append(_row_to_dict(r))
            # running 잡은 진행률이 계속 변하므로 항상 포함
            running = [_row_to_dict(r) for r in rows if r["status"] == "running"]
            payload = {c["id"]: c for c in changed}
            for r in running:
                payload[r["id"]] = r
            if payload:
                yield f"data: {json.dumps(list(payload.values()))}\n\n"
            time.sleep(0.5)

    return StreamingResponse(gen(), media_type="text/event-stream")
```

또한 `app/convert.py`에 바이트 해시 헬퍼 추가:

```python
def _sha256_bytes(data: bytes) -> str:
    return hashlib.sha256(data).hexdigest()
```

- [ ] **Step 4: 통과 확인**

Run: `cd pdf2md && python -m pytest tests/test_pdf2md.py -x -q`
Expected: PASS (전체)

- [ ] **Step 5: 커밋**

```bash
git add app/web.py app/convert.py tests/test_pdf2md.py
git commit -m "feat: FastAPI 업로드·목록·다운로드·미리보기·SSE + 가드레일·캐시·세션격리"
```

---

### Task 5: 정적 UI (노션풍) + frontend-design

**Files:**
- Create: `static/index.html`, `static/style.css`, `static/app.js`
- Create: `static/marked.min.js` (vendored)

**중요:** 이 태스크 착수 시 **`frontend-design` 스킬을 먼저 invoke**해서 노션풍(밝고 부드럽게) 시각 방향을 확정한 뒤 아래 골격에 스타일을 입힌다. 아래 코드는 동작하는 기능 골격이며, frontend-design이 색·간격·타이포·모션을 다듬는다.

**Interfaces:**
- Consumes: `POST /api/jobs`, `GET /api/jobs`, `GET /api/events`(SSE), `GET /api/jobs/{id}/preview`, `GET /api/jobs/{id}/download`.

- [ ] **Step 1: marked.js vendoring**

Run:
```bash
cd pdf2md && curl -sL https://cdn.jsdelivr.net/npm/marked/marked.min.js -o static/marked.min.js && head -c 40 static/marked.min.js && echo
```
Expected: marked UMD 번들 첫 바이트 출력. (오프라인이면 npm `marked` dist에서 복사)

- [ ] **Step 2: `static/index.html` 작성**

```html
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>pdf2md — PDF를 마크다운으로</title>
  <link rel="stylesheet" href="/static/style.css">
</head>
<body>
  <header>
    <h1>PDF → Markdown</h1>
    <p class="sub">PDF를 올리면 표까지 살려 마크다운으로 바꿔드립니다.</p>
  </header>

  <main>
    <section id="drop" class="dropzone">
      <input type="file" id="file" accept="application/pdf" multiple hidden>
      <p>여기에 PDF를 끌어다 놓거나 <button id="pick" class="link">파일 선택</button></p>
      <div class="opts">
        <label><input type="checkbox" id="opt-img" checked> 이미지 포함</label>
        <label><input type="checkbox" id="opt-csv" checked> 표 CSV 포함</label>
      </div>
    </section>

    <ul id="jobs" class="jobs"></ul>
  </main>

  <div id="modal" class="modal hidden">
    <div class="modal-box">
      <button id="modal-close" class="close">×</button>
      <div id="preview" class="preview markdown"></div>
    </div>
  </div>

  <script src="/static/marked.min.js"></script>
  <script src="/static/app.js"></script>
</body>
</html>
```

- [ ] **Step 3: `static/app.js` 작성**

```javascript
const $ = (s) => document.querySelector(s);
const drop = $("#drop"), fileInput = $("#file");

$("#pick").onclick = () => fileInput.click();
fileInput.onchange = () => upload([...fileInput.files]);

["dragover", "dragenter"].forEach(e =>
  drop.addEventListener(e, ev => { ev.preventDefault(); drop.classList.add("over"); }));
["dragleave", "drop"].forEach(e =>
  drop.addEventListener(e, ev => { ev.preventDefault(); drop.classList.remove("over"); }));
drop.addEventListener("drop", ev => upload([...ev.dataTransfer.files]));

async function upload(files) {
  files = files.filter(f => f.name.toLowerCase().endsWith(".pdf"));
  if (!files.length) return;
  const fd = new FormData();
  files.forEach(f => fd.append("files", f));
  fd.append("include_images", $("#opt-img").checked);
  fd.append("include_tables_csv", $("#opt-csv").checked);
  await fetch("/api/jobs", { method: "POST", body: fd });
  await refresh();
}

const state = new Map();

function render() {
  const list = $("#jobs");
  list.innerHTML = "";
  [...state.values()].forEach(j => {
    const li = document.createElement("li");
    li.className = "job " + j.status;
    const badge = { queued: "대기", running: "변환 중", done: "완료", failed: "실패" }[j.status] || j.status;
    li.innerHTML = `
      <div class="job-head">
        <span class="name">${escape(j.filename)}</span>
        <span class="badge ${j.status}">${badge}</span>
      </div>
      <div class="bar"><i style="width:${j.progress || 0}%"></i></div>
      ${j.error ? `<div class="err">${escape(j.error)}</div>` : ""}
      <div class="actions">
        ${j.status === "done" ? `<button data-p="${j.id}">미리보기</button>
          <a href="/api/jobs/${j.id}/download">ZIP 다운로드</a>` : ""}
      </div>`;
    list.appendChild(li);
  });
  list.querySelectorAll("[data-p]").forEach(b =>
    b.onclick = () => showPreview(b.dataset.p));
}

async function refresh() {
  const rows = await (await fetch("/api/jobs")).json();
  rows.forEach(j => state.set(j.id, j));
  render();
}

function connectSSE() {
  const es = new EventSource("/api/events");
  es.onmessage = (e) => {
    JSON.parse(e.data).forEach(j => state.set(j.id, j));
    render();
  };
  es.onerror = () => { es.close(); setTimeout(connectSSE, 2000); };
}

async function showPreview(id) {
  const md = await (await fetch(`/api/jobs/${id}/preview`)).text();
  $("#preview").innerHTML = marked.parse(md);
  $("#modal").classList.remove("hidden");
}
$("#modal-close").onclick = () => $("#modal").classList.add("hidden");
$("#modal").onclick = (e) => { if (e.target.id === "modal") $("#modal").classList.add("hidden"); };

function escape(s) { const d = document.createElement("div"); d.textContent = s || ""; return d.innerHTML; }

refresh(); connectSSE();
```

- [ ] **Step 4: `static/style.css` 작성 (노션풍 기본 — frontend-design이 정련)**

```css
:root {
  --bg: #ffffff; --panel: #f7f6f3; --text: #37352f; --muted: #9b9a97;
  --line: #e9e8e5; --accent: #2383e2; --radius: 10px;
  --ok: #448361; --warn: #d9730d; --err: #e03e3e;
}
@media (prefers-color-scheme: dark) {
  :root { --bg:#191919; --panel:#252525; --text:#e9e8e5; --muted:#7d7c78;
          --line:#333230; --accent:#529cff; }
}
* { box-sizing: border-box; }
body { margin: 0; background: var(--bg); color: var(--text);
  font: 15px/1.6 -apple-system, "Segoe UI", "Noto Sans KR", sans-serif;
  max-width: 760px; margin: 0 auto; padding: 40px 20px; }
header h1 { font-size: 26px; margin: 0 0 4px; }
.sub { color: var(--muted); margin: 0 0 28px; }
.dropzone { border: 1.5px dashed var(--line); border-radius: var(--radius);
  background: var(--panel); padding: 40px 20px; text-align: center; transition: .15s; }
.dropzone.over { border-color: var(--accent); background: color-mix(in srgb, var(--accent) 6%, var(--panel)); }
.link { background: none; border: none; color: var(--accent); cursor: pointer; font: inherit; padding: 0; }
.opts { margin-top: 16px; display: flex; gap: 20px; justify-content: center; color: var(--muted); }
.jobs { list-style: none; padding: 0; margin: 24px 0 0; display: flex; flex-direction: column; gap: 10px; }
.job { border: 1px solid var(--line); border-radius: var(--radius); padding: 14px 16px; background: var(--bg); }
.job-head { display: flex; justify-content: space-between; align-items: center; }
.name { font-weight: 500; }
.badge { font-size: 12px; padding: 2px 10px; border-radius: 999px; background: var(--panel); color: var(--muted); }
.badge.done { color: var(--ok); } .badge.failed { color: var(--err); } .badge.running { color: var(--accent); }
.bar { height: 6px; background: var(--panel); border-radius: 999px; margin: 10px 0 6px; overflow: hidden; }
.bar i { display: block; height: 100%; background: var(--accent); transition: width .4s; }
.job.done .bar i { background: var(--ok); }
.err { color: var(--err); font-size: 13px; }
.actions { display: flex; gap: 12px; margin-top: 6px; }
.actions button, .actions a { font: inherit; color: var(--accent); background: none; border: none;
  cursor: pointer; text-decoration: none; padding: 0; }
.modal { position: fixed; inset: 0; background: rgba(0,0,0,.4); display: flex; align-items: center; justify-content: center; padding: 20px; }
.modal.hidden { display: none; }
.modal-box { background: var(--bg); border-radius: var(--radius); max-width: 720px; width: 100%;
  max-height: 85vh; overflow: auto; padding: 32px; position: relative; }
.close { position: absolute; top: 12px; right: 16px; font-size: 22px; background: none; border: none; cursor: pointer; color: var(--muted); }
.markdown table { border-collapse: collapse; } .markdown td, .markdown th { border: 1px solid var(--line); padding: 6px 10px; }
```

- [ ] **Step 5: 수동 스모크 테스트**

Run:
```bash
cd pdf2md && (PDF2MD_DATA=/tmp/pdf2md-smoke python -m app.worker &) && \
PDF2MD_DATA=/tmp/pdf2md-smoke uvicorn app.web:app --port 8001 &
sleep 3 && curl -s localhost:8001/ | grep -q "PDF → Markdown" && echo "UI OK"
```
Expected: `UI OK`. 브라우저(SSH 터널 8001)에서 `tests/fixtures/sample.pdf` 업로드 → 큐→변환중→완료, 미리보기·ZIP 동작 확인. 확인 후 두 프로세스 종료.

- [ ] **Step 6: 커밋**

```bash
git add static/
git commit -m "feat: 정적 UI(노션풍) — 드롭존·진행바·미리보기·SSE"
```

---

### Task 6: Docker 패키징 + 배포 설정

**Files:**
- Create: `requirements.txt`, `Dockerfile`, `docker-compose.yml`, `.env.example`, `.gitignore`
- Modify: `/home/dev/code/PORTS.md` (레지스트리 등록)

**Interfaces:** 없음 (배포 산출물).

- [ ] **Step 1: `requirements.txt` 작성**

```
fastapi
uvicorn[standard]
python-multipart
docling
pymupdf
```

> pandas는 docling 의존성으로 함께 설치된다(표 CSV용).

- [ ] **Step 2: `.gitignore` 작성**

```
data/
__pycache__/
*.pyc
.venv/
/tmp/
```

- [ ] **Step 3: `Dockerfile` 작성**

```dockerfile
FROM python:3.12-slim

# PyMuPDF·docling 런타임에 필요한 최소 라이브러리
RUN apt-get update && apt-get install -y --no-install-recommends \
    libgl1 libglib2.0-0 && rm -rf /var/lib/apt/lists/*

WORKDIR /srv
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Docling 레이아웃/TableFormer 모델을 빌드 타임에 받아 상주(런타임 다운로드 회피)
RUN python -c "from docling.utils.model_downloader import download_models; download_models()" || true

COPY app/ app/
COPY static/ static/

ENV PDF2MD_DATA=/data
EXPOSE 8001
CMD ["uvicorn", "app.web:app", "--host", "0.0.0.0", "--port", "8001"]
```

- [ ] **Step 4: `docker-compose.yml` 작성**

```yaml
services:
  web:
    build: .
    command: uvicorn app.web:app --host 0.0.0.0 --port 8001
    ports:
      - "${PDF2MD_PORT:-8001}:8001"
    volumes:
      - ./data:/data
    environment:
      PDF2MD_ADMIN_KEY: "${PDF2MD_ADMIN_KEY:-}"
      PDF2MD_SEC_PER_PAGE: "${PDF2MD_SEC_PER_PAGE:-1.5}"
    mem_limit: 2g
    restart: unless-stopped

  worker:
    build: .
    command: python -m app.worker
    volumes:
      - ./data:/data
    environment:
      PDF2MD_SEC_PER_PAGE: "${PDF2MD_SEC_PER_PAGE:-1.5}"
    mem_limit: 3g
    restart: unless-stopped
```

- [ ] **Step 5: `.env.example` 작성**

```
# 관리자 전체조회 키(비우면 관리자 기능 비활성)
PDF2MD_ADMIN_KEY=
# host 포트 (PORTS.md 규칙: NN=01)
PDF2MD_PORT=8001
# 진행률 추정용 초/페이지 (실측으로 보정)
PDF2MD_SEC_PER_PAGE=1.5
```

- [ ] **Step 6: compose 검증 + 빌드**

Run: `cd pdf2md && docker compose config -q && echo "compose OK" && docker compose build web`
Expected: `compose OK`, 이미지 빌드 성공 (모델 다운로드로 수 분 소요).

- [ ] **Step 7: 기동 스모크**

Run:
```bash
cd pdf2md && docker compose up -d && sleep 20 && \
curl -s localhost:8001/ | grep -q "PDF → Markdown" && echo "SERVE OK" && docker compose logs --tail 5 worker
```
Expected: `SERVE OK`, 워커 로그에 크래시 없음. 브라우저에서 실제 정부보고서 PDF 하나로 표 변환 품질·진행바 확인.

- [ ] **Step 8: PORTS.md 등록**

`/home/dev/code/PORTS.md` 레지스트리 표에 한 줄 추가:

```
| 01 | pdf2md | 8001 | — | — | — | — |
```

그리고 "비어 있는 NN" 줄에서 `01` 제거.

- [ ] **Step 9: 커밋**

```bash
git add requirements.txt Dockerfile docker-compose.yml .env.example .gitignore
git commit -m "feat: Docker 패키징(web+worker) + 배포 설정"
cd /home/dev/code && git add PORTS.md && git commit -m "docs: PORTS 레지스트리에 pdf2md(NN=01) 등록"
```

---

## Self-Review

**Spec coverage:**
- 엔진 Docling(OCR off, ACCURATE) → Task 2 ✓
- 2서비스 단일이미지, SQLite 큐 → Task 1,6 ✓
- 다중 업로드·순차 큐·진행상황(SSE) → Task 3,4,5 ✓
- 익명 세션 + admin key → Task 4 ✓
- 이미지 ZIP(기본)/이미지 생략 옵션 → Task 2,4,5 ✓
- 해시 캐시 → Task 1(find_cached),4(업로드 경로) ✓
- 표 CSV 추출 → Task 2 ✓
- 미리보기 → Task 4,5 ✓
- 가드레일(100MB/500p/매직바이트/큐20) → Task 4 ✓
- 보관 24h + 참조카운트 정리 → Task 3 ✓
- 노션풍 밝은 UI → Task 5 ✓
- 포트 8001/NN=01 → Task 6 ✓
- 테스트 4종(캐시·비PDF·상태전이·세션격리) → Task 1~4 ✓

**Placeholder scan:** 없음. 모든 코드 스텝에 실제 코드 포함.

**Type consistency:** `opts_hash(inc_img, inc_csv)` 시그니처 Task2/3/4 일치. `convert.convert(pdf, out, include_images=, include_tables_csv=)` 일치. `_sha256_bytes`는 Task2에서 정의(Task4 사용). `db.*` 시그니처 Task1 정의와 Task3/4 호출 일치.

**주의(구현 시 확인):** ① `save_as_markdown`의 아티팩트 폴더명이 docling 버전에 따라 `{stem}_artifacts`가 아닐 수 있음 → Task2 Step5 실 변환에서 실제 폴더명 확인 후 `convert.py` 보정. ② `export_to_dataframe(doc=doc)` 인자 형태 버전차 → 런타임 확인. ③ `download_models()` 경로는 docling 버전별 상이 가능 → 실패해도 런타임 다운로드로 폴백(`|| true`).
